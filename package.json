{
  "name": "match_three",
  "version": "1.0.0",
  "description": "A simple match three puzzle game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass --watch scss/ --o dist/css --recursive"
  },
  "author": "Travis Reynolds",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^2.0.1",
    "node-sass": "^4.9.4"
  }
}
